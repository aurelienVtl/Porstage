{% extends 'base.html.twig' %}
{% block contenu %}
	<div class=""container-fluid"">
		<h3> 
			ajouter une Stage !
		</h3>
	</div>
	<div class="row">
		<div class="col-md-2">
		</div>
		<div class="col-md-8">
			{{ form_start(vueFormualaire) }}
				{{ form_row(vueFormualaire.titre) }}
				{{ form_row(vueFormualaire.mission) }}
				{{ form_row(vueFormualaire.emailContact) }}
				{{ form_row(vueFormualaire.entreprise) }}
				{{ form_row(vueFormualaire.formations) }}
				<button type="submit" class="btn btn-dark" id="boutonForm"> Enregistrer </button>
			{{ form_end(vueFormualaire) }}
		</div>
		<div class="col-md-2">
		</div>
	</div>
	

{% endblock %}
