{% extends 'base.html.twig' %}
{% block contenu %}
<div class=""container-fluid"">
    <h3> YO </h3>
</div>
{{ form_start(vueFormualaire) }}
	{{ form_row(vueFormualaire.nom) }}
	{{ form_row(vueFormualaire.adresse) }}
	{{ form_row(vueFormualaire.activite) }}
	{{ form_row(vueFormualaire.urlsite) }}
	<button type="submit" class="btn btn-dark"> Enregistrer </button>
{{ form_end(vueFormualaire) }}
	

{% endblock %}
