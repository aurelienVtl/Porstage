{% extends 'base.html.twig' %}

{% block title %}Inscrivez vous{% endblock %}

{% block contenu %}

    {{ form_start(vueFormulaire) }}
		{{ form_row(vueFormulaire.prenom) }}
		{{ form_row(vueFormulaire.nom) }}
		{{ form_row(vueFormulaire.username) }}
		{{ form_row(vueFormulaire.password) }}
		<button type="submit" class="btn btn-dark" id="boutonForm"> Enregistrer </button>
	{{ form_end(vueFormulaire) }}
{% endblock %}
